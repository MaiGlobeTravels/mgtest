# City Tours (Vue Frontend)

Simple Vue 3 + Vite frontend for the City Tours API.

Setup

```bash
cd city-tours_fe
npm install
npm run dev
```

The dev server proxies `/api` to `http://localhost` (adjust in `vite.config.js`).
